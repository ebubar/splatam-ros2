name: splatam
channels:
  - nvidia/label/cuda-11.6.0
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - cuda-toolkit
  - cudatoolkit=11.6
  - python=3.10
  - pytorch=1.12.1
  - torchaudio=0.12.1
  - torchvision=0.13.1
  - tqdm=4.65.0
  - Pillow
  - faiss-gpu
  - opencv
  - imageio
  - matplotlib
  - kornia
  - natsort
  - pyyaml
  - wandb
  - gxx_linux-64=10
  - pip
  - pip:
    - lpips
    - open3d==0.16.0
    - torchmetrics
    - cyclonedds
    # CUDA rasterizer (research/non-commercial) -> "cuda" fallback backend only.
    - git+https://github.com/JonathonLuiten/diff-gaussian-rasterization-w-depth.git@cb65e4b86bc3bd8ed42174b72a62e8d3a3a71110
    # gsplat (Apache-2.0) -> default engine for scripts/zed2i_gsplat_live.py.
    # Set TORCH_CUDA_ARCH_LIST for your GPU before `conda env create`.
    - gsplat==1.4.0
