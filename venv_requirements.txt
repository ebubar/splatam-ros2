tqdm==4.65.0
Pillow
imageio
matplotlib
kornia
natsort
pyyaml
wandb
lpips
torchmetrics
cyclonedds
pytorch-msssim
plyfile==0.8.1
numpy==1.26.4
# CUDA rasterizers are installed via bash_scripts/install.bash (arch-aware), NOT here.
# gsplat (Apache-2.0) = default engine; diff-gaussian-rasterization = optional cuda fallback
# built from third_party/diff-gaussian-rasterization. See requirements.txt header.